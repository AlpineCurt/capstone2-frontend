import React from "react";

const NonHostView = () => {
    return (
        <div className="HostView">
            Waiting for the host to start the game
        </div>
    );
}

export default NonHostView;